# Application/runtime configuration (copy to .env for local development)

# Supabase project URL (https://<project-ref>.supabase.co)
SUPABASE_URL=

# Supabase anonymous key for client-side or low-privileged access (do not use in server-only code if a service role is available)
SUPABASE_ANON_KEY=

# Supabase service role key for server-side operations (keep secret; never commit). Optional depending on usage.
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI API key for packages that integrate with OpenAI
OPENAI_API_KEY=

# SQLAlchemy-compatible database URL (e.g., postgres://user:pass@host:5432/dbname)
DATABASE_URL=

# Common runtime knobs
# Environment name: local | staging | production
APP_ENV=local
# Host/port defaults for local FastAPI apps
API_HOST=127.0.0.1
API_PORT=8000
# Logging level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO
